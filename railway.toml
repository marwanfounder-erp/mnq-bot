[build]
builder = "NIXPACKS"

[deploy]
startCommand = "bash start.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Health check: Railway pings / to confirm the dashboard is up
healthcheckPath = "/"
healthcheckTimeout = 30
